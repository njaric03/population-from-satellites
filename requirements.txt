# Zavisnosti projekta. Verzije su one na kojima je projekat pokretan
# (Databricks ML runtime, jul 2026); >= a ne ==, jer Databricks dolazi sa
# svojim, novijim buildovima koje nema smisla degradirati.
#
# Notebooci na Databricksu instaliraju samo ono sto runtime nema
# (`%pip install -q timm mlflow`); ovaj fajl je za lokalni rad i za evidenciju
# sta je tacno potrebno.
#
#   pip install -r requirements.txt

# --- trening i evaluacija (notebooci) ---
torch>=2.2
torchvision>=0.17
timm>=1.0
scikit-learn>=1.4
numpy>=1.26
pandas>=2.2
pyarrow>=15.0            # pandas parquet engine (OOF predikcije)
matplotlib>=3.8
mlflow>=3.0              # mlflow.pytorch.log_model(name=...) postoji tek od 3.0

# --- priprema podataka (scripts/) i EDA ---
geopandas>=1.0
shapely>=2.0
pyogrio>=0.9             # ucitavanje .gpkg geometrija
rasterio>=1.3            # rasterizacija otisaka zgrada, citanje kompozita
openpyxl>=3.1            # RZS popis .xlsx
openeo>=0.30             # Sentinel-2 iseci preko Copernicus Data Space

# --- notebooci ---
jupyter>=1.0
